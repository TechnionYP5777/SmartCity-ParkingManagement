package gui.driver.shaharTesting;

/*
import javafx.collections.FXCollections;
import javafx.collections.ObservableList;
import javafx.geometry.Pos;
import javafx.scene.Scene;
import javafx.scene.control.Button;
import javafx.scene.control.Label;
import javafx.scene.control.ListView;
import javafx.scene.control.cell.ComboBoxListCell;
import javafx.scene.layout.GridPane;
import javafx.scene.layout.VBox;
import javafx.stage.Modality;
import javafx.stage.Stage;
*/
public class ListViewTesting {
	/*
	 * public static final ObservableList<String> names =
	 * FXCollections.observableArrayList(); public static final
	 * ObservableList<String> data = FXCollections.observableArrayList();
	 * 
	 * public static void display(String title, String message){ Stage window =
	 * new Stage(); window.initModality(Modality.APPLICATION_MODAL);
	 * window.setTitle(title); window.setMinWidth(250); window.setHeight(300);;
	 * ListView<String> list = new ListView<String>(); ObservableList<String>
	 * items =FXCollections.observableArrayList ( "Single", "Double", "Suite",
	 * "Family App"); list.setItems(items);
	 * 
	 * for (int i = 0; i < 18; i++) { data.add("anonym"); } //
	 * items.addAll(data); list.setItems(data);
	 * list.setCellFactory(ComboBoxListCell.forListView(items));
	 * 
	 * Label label = new Label(); label.setText(message); Button button = new
	 * Button("close"); button.setOnAction(e-> window.close()); VBox layout =
	 * new VBox(); layout.getChildren().addAll(label, button);
	 * layout.setAlignment(Pos.CENTER);
	 * 
	 * GridPane root = new GridPane(); root.add(list, 1, 1);
	 * //root.getChildren().add(list); window.setScene(new Scene(root, 200,
	 * 250)); window.show(); }
	 */
}
